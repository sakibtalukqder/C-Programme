#include <stdio.h>

int main() {
    int age;
    /*
    // ----------------- Code 01 -----------------

    int arr[3][2]={{1,2},{3,4},{5,6}};
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 2; j++) {
            printf("arr[%d][%d] : %d \n", i, j, arr[i][j]);
        }

    }

    // ----------------- Code 02 -----------------

    int i,arr[5];
    for (i = 0; i < 5; i++) {
        printf("Enter Element arr[%d]: ",i);
        scanf("%d", &arr[i]);
        printf("\n");
    }
    for (i = 0; i < 5; i++) {
        printf("Element of arr[%d]=%d \n",i,arr[i]);
    }
    */
}